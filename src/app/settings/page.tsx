'use client';
import { ComingSoon } from '@/components/ui/ComingSoon';

export default function SettingsPage() {
  return <ComingSoon title="Account Settings" description="Account settings are currently unavailable. We are migrating to a more robust preferences center with advanced controls." />;
}
